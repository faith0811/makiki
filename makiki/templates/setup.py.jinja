# -*- coding: utf-8 -*-

from setuptools import setup, find_packages

setup(
    name='',
    version='0.0.1',
    description='',
    long_description='',
    author='',
    author_email='',
    packages=find_packages(),
    url='',
    include_package_data=True,
    entry_points={
        'console_scripts': [],
    },
    zip_safe=False,
    install_requires=[],
)
